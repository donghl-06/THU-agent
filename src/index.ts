/**
 * THU Assistant Agent 的 pnpm dev 提示入口。
 *
 * 实际 CLI、Web、Skill 与 MCP 启动入口独立装配，见 docs/architecture.md。
 */
console.log("清灵启动命令：pnpm agent（终端对话）、pnpm web（网页）、pnpm --silent skill list（外部 Agent 工具）、pnpm --silent mcp（MCP）。架构见 docs/architecture.md。");
