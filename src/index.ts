/**
 * THU Assistant Agent 入口。
 *
 * 当前处于 Bottom-up 开发的早期阶段（见 ROADMAP.md）：
 * 这里暂时只是占位，验证项目能跑。
 * 后续 Harness 完成后，这里会成为 Agent 的启动入口。
 */
console.log("THU Assistant Agent 脚手架就绪。下一步见 ROADMAP.md 的 Step 1。");
