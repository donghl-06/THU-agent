# 清华小助手前端 UI / UX 升级任务

你现在需要对项目中的"清华小助手"聊天页面进行一次较完整的 UI/UX 重构。

当前页面功能已经基本可用，但 UI 仍然偏初版/开发版，希望将其升级为一个成熟、现代、简洁、有品质感的 AI Assistant Web App。

请优先基于现有项目代码进行修改，不要重写整个项目，不要破坏现有后端接口、登录逻辑、Agent 调用逻辑和已有功能。

## 核心目标

在保留"清华紫"作为核心品牌色的前提下，让整个产品从"Demo 页面"提升到"可以真正给学生长期使用的校园 AI 助手"。

## 一、总体设计原则

请遵循以下原则进行设计：

### 1. 保留清华紫作为主品牌色

不要更换当前产品的核心主题。

推荐继续使用类似（Tsinghua Purple）：

- `#82318E`
- `#8F3FA3`
- `#A855F7`

具体颜色可以根据现有项目进行调整，但整体视觉必须让用户第一眼感觉到：**清华 / 校园 / 科技 / AI**。

不要把整个页面大面积涂成紫色。清华紫主要用于：

- Primary Button
- 用户消息气泡
- 激活状态
- Logo / 品牌元素
- loading / progress
- Hover
- Focus
- 少量渐变高光

其余区域使用：

- 深灰
- 炭黑
- 浅灰
- 白色
- 半透明背景

形成比较克制的深色 UI。

视觉方向可以参考成熟 AI 产品的感觉：ChatGPT、Claude、Perplexity、Linear、Notion AI、Arc。但不要简单复制任何一个产品。

## 二、首先解决当前页面几个明显的问题

目前页面存在以下 UI 问题，请优先处理。

### 2.1 页面横向过宽

现在聊天内容几乎横跨整个屏幕。

在大屏幕上：

- 用户消息离右边太近
- AI 消息宽度过大
- 阅读长文本很累
- 页面中央存在大量无意义空白

请建立统一的聊天内容容器，例如：

```css
max-width: 960px ~ 1100px;
margin: 0 auto;
```

让对话集中在屏幕中央。

### 2.2 优化消息气泡

重新设计 User / Assistant 两种消息。

**User Message** 建议：

- 靠右
- 清华紫背景
- 紫色可以有轻微渐变
- 白色文字
- 最大宽度约 70%
- `border-radius` 16px~20px
- 合理 padding
- 不要过于鲜艳

例如视觉：

```text
                         ┌─────────────────────────┐
                         │ 帮我看看晚上还有哪些   │
                         │ 体育场馆有空位          │
                         └─────────────────────────┘
```

**Assistant Message**：不要再像现在这样使用一个非常厚重的大灰色矩形。

建议：

- 靠左
- 深灰 / 半透明 card
- subtle border
- 更轻的背景
- 最大宽度 75%~85%
- 行高明显增加
- Markdown 层级清楚

长回答中的：标题、列表、地点、空位数量、时间应该有明显层级。

## 三、重新设计顶部 Header

现在顶部 Header 信息比较零散。

建议改成一个更加成熟的 App Header。

**左侧：**

> [清华小助手 Logo]
>
> 清华小助手
> Tsinghua AI Assistant

或者：

> 清华小助手
> AI Campus Assistant

不要再放非常长的：

> 单用户本地模式 · 写操作会弹窗确认 · 支持语音/图片输入

这些属于开发信息，可以：删除、移入设置、或放到 tooltip。

**右侧保留：**

- 登录状态
- 通知
- 设置
- 用户头像

例如：

```text
清华小助手                             ● 已连接  🔔  ⚙  [头像]
```

Header 可以：

```css
position: sticky;
top: 0;
backdrop-filter: blur(...);
```

增加轻微毛玻璃效果。

## 四、重点升级：Agent 思考过程反馈

**这是本次改版最高优先级功能之一。**

当前最大 UX 问题：用户发送消息后，Agent 在后台执行期间页面没有任何明显变化。

用户不知道：

- 消息是否成功发送
- Agent 是否收到问题
- 是否正在调用工具
- 是否卡住
- 是否仍然工作

必须解决。

## 五、消息发送后的完整状态设计

用户点击发送后，立即执行：

1. 用户消息立即出现在聊天窗口
2. 自动滚动到底部
3. 出现 Assistant Loading Card
4. 显示动态状态
5. 后端返回结果后替换 Loading Card

不要等 API 返回后才改变页面。

## 六、设计 Assistant Thinking / Loading Card

例如：

```text
┌────────────────────────────────────┐

 ✦ 清华小助手正在处理

 ● 正在理解你的问题
 ○ 正在查询校园信息
 ○ 正在整理结果

        ·  ·  ·

 已处理 3s

└────────────────────────────────────┘
```

其中 ● 可以使用紫色动态 pulse。

**动态 Loading 文案**：根据 Agent 状态，可以轮换：

- 正在理解你的问题…
- 正在判断需要使用的校园服务…
- 正在查询相关信息…
- 正在整理查询结果…
- 正在生成回答…

如果后端已经能够返回 Agent / Tool 状态，则应该直接展示真实状态。例如：

```text
✓ 已理解问题
✓ 正在查询体育场馆
● 正在读取场馆空闲情况
○ 正在整理结果
```

## 七、增加 Typing Indicator

在等待 LLM 输出时增加类似：

```text
清华小助手正在思考  ● ● ●
```

三个点使用轻量动画（循环 bounce / opacity）。动画不要太夸张。

## 八、支持 Streaming Response

如果后端支持流式输出，请优先采用流式回答。

即：

> 查到了，目前晚上还有……

逐步出现在页面，而不是等待完整回答后一次性出现。

这样即使整个任务需要 20 秒：3 秒后用户已经开始看到内容，用户体验会远远优于"20 秒页面不动 → 突然出现完整答案"。

如果后端暂时不支持 streaming：先完成 Loading / Thinking 状态。**不要为了 Streaming 大规模破坏后端。**

## 九、设计 Agent Tool Call 状态

这个项目不是普通 Chatbot，而是 Campus Agent。

因此建议适当体现"AI 正在替你调用校园服务"。例如：

```text
┌───────────────────────────┐
│ 🔍 正在查询体育场馆       │
│                           │
│ 场馆服务                  │
│ 正在获取今晚的空闲信息…   │
└───────────────────────────┘
```

或者更轻量：

```text
⌁ 正在查询「体育场馆」
```

完成后：

```text
✓ 体育场馆查询完成
```

建议不要把 Function Calling、MCP、Tool、API、JSON 这些开发术语直接暴露给普通用户。

## 十、增加 Progress / Activity UI

对于耗时较长的任务，可以增加一个细小的动态进度条。

例如 Assistant Loading Card 顶部：

```text
━━━━━━━━━━━━━━━━━━━━
```

清华紫渐变不断流动。

注意：如果后端没有真实 progress，不要显示虚假的 37%、68%、92%。使用 Indeterminate Progress Bar（无限动态 loading bar）。

## 十一、增加"停止生成"

当 Agent 工作时，原来的发送按钮"发送"变为"■ 停止"，用户可以主动取消。任务结束后恢复"发送"。

## 十二、输入框彻底升级

目前输入框比较像普通 HTML 输入栏。改造成成熟 AI App 的 Composer。

建议结构：

```text
┌──────────────────────────────────────────────────────┐
│ 问点什么，例如：今晚哪里可以打羽毛球？               │
│                                                      │
│ 📎  🎤                                      ➤        │
└──────────────────────────────────────────────────────┘
```

支持：

- `Enter` 发送
- `Shift+Enter` 换行

输入框要求：

- auto resize
- 最大高度限制
- focus 有轻微清华紫 glow
- placeholder 降低亮度
- border 不要太重

## 十三、重新设计附件和语音按钮

现在左下角两个 icon 不够直观。

建议：

- 📎 附件
- 🎤 语音

Desktop 默认只显示 icon。Hover 时显示 tooltip（"上传图片或文件" / "语音输入"）。

所有 icon：统一尺寸、统一 stroke、统一 hover、统一圆角按钮。

建议使用项目已经存在的 Icon Library。如果没有，可以考虑 Lucide Icons。不要混用 emoji + SVG + unicode 图标。

## 十四、增加 Empty State / Welcome Page

新用户刚打开页面时，不要直接出现大片聊天记录。

设计一个 Welcome State：

```text
                ✦

           清华小助手

      今天想在清华做点什么？

 ┌─────────────┐ ┌─────────────┐
 │ 📚 查询课程 │ │ 🏃 场馆空位 │
 └─────────────┘ └─────────────┘

 ┌─────────────┐ ┌─────────────┐
 │ 🗓 空闲时间 │ │ 🏫 校园服务 │
 └─────────────┘ └─────────────┘
```

例如推荐问题：

- 我今天有什么课？
- 今晚哪里可以打羽毛球？
- 帮我看看图书馆哪里有位置
- 我明天下午有什么空闲时间？

点击后自动填入输入框或者直接发送。

这会让产品从"一个聊天框"变成"校园 AI Assistant"。

## 十五、区分系统消息和 AI 消息

目前"登录成功！""请先点击右上角登录"这些内容看起来像普通 AI 回复，不合理。

这些应该改为 **Toast**，例如右上角：

```text
✓ 登录成功
已连接清华 Info
```

2~3 秒后消失。

或者 **System Notice**，非常轻量地显示：

```text
──── 已成功连接清华 Info ────
```

不要使用普通 Assistant Bubble。

## 十六、彻底优化错误 UI

目前错误直接展示：

```text
LLM 接口报错（HTTP 403）：
{"error":{"message":"You've reached..."}}
```

这属于开发日志，普通用户不应该看到 JSON。

应该转换成：

```text
┌────────────────────────────────┐
│ ⚠️ 暂时有点忙                  │
│                                │
│ 当前请求较多，请稍后再试。     │
│                                │
│        [重新尝试]              │
└────────────────────────────────┘
```

开发环境可以展开"技术详情"，里面再显示 HTTP 403、concurrent request limit。生产环境默认折叠。

## 十七、增加 Retry

如果请求失败，直接在该消息下面显示：

```text
↻ 重新生成
```

不要要求用户"重新复制问题 → 重新发送"。

## 十八、增加常用 Message Action

AI 回复 Hover 时可以显示：复制、重新生成、👍、👎。

例如：

```text
📋  ↻  👍  👎
```

默认隐藏，Hover 才出现，避免页面太乱。

## 十九、优化 Markdown

Agent 的结果很可能包含：列表、表格、时间、地点、数字、链接。

因此必须专门优化 Markdown 样式，尤其是：

- H1 / H2 / H3
- ul
- ol
- table
- strong
- code
- blockquote

目前类似 `**北馆**` 这种 Markdown 标记不应该直接裸露给用户。确认前端正确解析 Markdown。

## 二十、校园数据使用 Card 展示

对于结构化校园信息，不应该永远只返回纯文本。

例如体育场馆，可以展示：

```text
体育场馆
──────────────────

紫荆操场

开放时间
18:00 - 22:00

当前状态
● 有空位

[查看详情]
```

图书馆：

```text
李文正馆 · 三层 B 区

 93        140      34%
剩余座位   总座位   当前占用率

[查看位置]
```

后续可以逐渐引入：LibraryCard、CourseCard、VenueCard、ScheduleCard、ServiceCard。

第一阶段不需要全部完成，先建立统一 Card Design System。

## 二十一、减少纯文本，提高信息扫描效率

例如现在：

```text
排名前几的还有：
1. 文科图书馆……
2. 西馆……
3. 法律图书馆……
```

可以逐渐变成：

```text
空闲座位较多

李文正馆 B区             93
文科图书馆               83
西馆                     82
法律图书馆               81
```

重点数字右对齐。用户能够 1 秒钟找到答案。

## 二十二、增加轻量动画

建议加入：

- message fade-in
- message slide-up
- button hover
- card hover
- loading pulse
- typing dots
- progress shimmer
- toast animation

所有动画：150ms ~ 300ms。

不要出现：大幅缩放、页面乱飞、强烈弹跳、过度霓虹。

产品应该是：稳定、轻盈、自然。

## 二十三、统一 Border Radius

建立 Design Token，例如：

| Token  | 值    |
| ------ | ----- |
| Small  | 8px   |
| Medium | 12px  |
| Large  | 16px  |
| XL     | 20px  |

不要页面有的 4px、有的 7px、有的 20px、有的 30px。

## 二十四、统一间距体系

建议使用 4 / 8 / 12 / 16 / 24 / 32 / 48 作为 spacing scale。

统一 padding、margin、gap，减少现在比较随意的空间感。

## 二十五、优化字体和文字层级

建立 Typography System：

| 层级       | 规格               |
| ---------- | ------------------ |
| 页面标题   | 20~24px / semibold |
| Card Title | 16px / medium      |
| 正文       | 14~16px            |
| Secondary  | 13~14px            |
| Caption    | 12px               |

正文建议 `line-height: 1.6 ~ 1.8`，AI 长文本会明显更舒服。

## 二十六、增加背景层次

不要纯 `#000000`。

建议采用多层深灰：

| 区域            | 颜色     |
| --------------- | -------- |
| Page Background | #111113  |
| Header          | #18181B  |
| Assistant Card  | #202024  |
| Hover           | #29292E  |

并搭配 `rgba(...)`、border、shadow、blur。清华紫只作为强调。

这样会比"黑色 + 大紫色块"成熟很多。

## 二十七、增加非常轻微的品牌氛围

页面背景可以有 radial-gradient，例如右上或顶部极低透明度紫色 glow：

```css
rgba(130, 49, 142, 0.08)
```

不要明显。目标是用户感觉页面"不再是一整块死黑"，但又不会注意到一个明显紫色光圈。

## 二十八、优化滚动

实现：

- 发送消息 → 自动滚动到底部
- Streaming → 跟随输出

但如果用户主动往上翻：停止强制 scroll，此时右下角出现"↓ 回到底部"，点击恢复。

## 二十九、登录状态重新设计

目前顶部"已登录"可以升级为：

```text
● 清华 Info 已连接
```

Hover 显示"当前账号已完成认证"，绿色小点表示服务连接正常。

如果失效：

```text
● 认证已过期
重新登录
```

不要通过聊天消息提示"请先点击右上角登录"。

## 三十、移动端 / 小屏幕适配

请同时检查以下宽度：

- 1920
- 1440
- 1280
- 1024
- 768
- 375

至少保证 Desktop / Tablet / Mobile 都可以正常使用。

尤其确保：

- 输入框始终可见
- 消息不会超过屏幕
- 按钮不会重叠
- Header 不爆炸

## 三十一、建议的最终页面结构

推荐最终 DOM / Component 逻辑：

```text
App
│
├── AppHeader
│
├── ChatContainer
│   │
│   ├── WelcomeScreen
│   │
│   └── MessageList
│       │
│       ├── UserMessage
│       ├── AssistantMessage
│       ├── ThinkingMessage
│       ├── ToolStatus
│       └── ErrorMessage
│
├── ScrollToBottomButton
│
├── ChatComposer
│   ├── AttachmentButton
│   ├── VoiceButton
│   ├── TextArea
│   └── SendButton
│
└── ToastContainer
```

尽量组件化。不要把所有代码继续堆在一个页面 Component 里面。

## 三十二、建议建立 Design Tokens

例如：colors、spacing、radius、font-size、shadow、transition，集中维护。

如果当前项目已经使用 Tailwind、CSS Variables、Theme Provider，优先沿用现有系统，不要重复造一套。

## 三十三、开发优先级

不要一次修改几千行然后再运行。按照以下顺序实施。

### P0 — 首先解决 UX

- 聊天区域布局
- 消息 Bubble
- 输入框
- Thinking 状态
- Loading Indicator
- Error Card
- Retry
- Toast
- 自动滚动

### P1 — 产品化

- Header
- Welcome Screen
- Quick Actions
- Markdown
- Tool Status
- Message Action
- Structured Card

### P2 — 精致化

- 动画
- 渐变
- 毛玻璃
- Hover
- 背景 glow
- 响应式
- 细节 polish

## 三十四、特别重要：Thinking 状态逻辑

请仔细检查当前 `sendMessage()`、Agent Request、API Request、Loading State 相关实现。

不要简单做一个假的 CSS Loading。

应该真正建立请求生命周期：

```text
idle
↓
sending
↓
thinking
↓
tool_calling
↓
generating
↓
success
```

异常：`error`。取消：`cancelled`。

前端 UI 根据状态变化。

如果当前后端只支持 request → response，那么先实现 `idle` / `sending` / `thinking` / `success` / `error`，以后再接入 `tool_calling` / `generating`。

## 三十五、请求期间建议的用户体验

例如用户发送"帮我看看晚上还有哪些体育场馆有空位"，页面应该立即：

```text
USER
帮我看看晚上还有哪些体育场馆有空位
```

随后：

```text
✦ 清华小助手正在处理

● 正在理解你的需求…
```

约 1 秒后：

```text
✓ 已理解需求
● 正在查询体育场馆信息…
```

如果有真实 Tool Event：

```text
✓ 体育场馆数据获取完成
● 正在整理今晚可用场馆…
```

随后开始 Streaming：

```text
今晚目前有这些场馆仍然有空位：
...
```

整个过程中用户永远不应该面对"发出消息以后屏幕十几秒完全没有变化"。

## 三十六、不要做的事情

请避免：

- ❌ 大面积高饱和紫色
- ❌ 大量渐变
- ❌ 赛博朋克
- ❌ 大量发光
- ❌ 每个东西都是 Card
- ❌ 每个按钮都有动画
- ❌ 过多 Emoji
- ❌ 把 Agent 内部技术细节暴露给用户
- ❌ 在没有真实进度的情况下伪造百分比进度
- ❌ 为了 UI 重构破坏现有 API
- ❌ 一次性重写整个前端

产品视觉关键词：简洁、高级、克制、校园、AI、现代、清晰、可信赖。

而不是：炫技、赛博朋克、Demo、Dashboard。

## 三十七、实施要求

开始写代码之前，请先：

1. 阅读当前前端目录结构
2. 找到聊天页面对应组件
3. 找到消息发送逻辑
4. 找到请求 Agent / LLM 的逻辑
5. 找到登录状态逻辑
6. 找到当前 CSS / Tailwind / UI Library
7. 判断哪些已有组件可以复用

然后先向我输出：

- 当前 UI 架构分析
- 计划修改的文件
- 准备新增的组件
- 准备修改的状态管理
- 具体实施步骤

确认整体方案之后再逐步实施。

每完成一个阶段都确保 `npm run build` 或当前项目对应构建命令可以正常运行。

不要为了 UI 优化修改无关业务逻辑。

## 最终验收标准

**视觉层面**：第一眼看上去像一个已经发布、有人维护的成熟 AI 产品，而不是课程作业或 Hackathon Demo。

**信息层面**：用户能够很快区分"我说的话""AI 回复""系统通知""Agent 正在执行""发生错误"。

**交互层面**：用户点击发送后的 100~300ms 内，页面必须产生明确视觉反馈。

**等待体验**：即使 Agent 需要 10~30 秒完成任务，用户也始终知道系统仍然正在工作。

**品牌层面**：明显保留清华紫，但不过度使用。

**工程层面**：不破坏原有业务功能，组件结构比改造前更加清晰，后续方便继续增加课表、图书馆、体育场馆等校园服务组件。
